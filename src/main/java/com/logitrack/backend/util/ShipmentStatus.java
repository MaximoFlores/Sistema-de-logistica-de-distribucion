package com.logitrack.backend.util;

public enum ShipmentStatus {
    PENDING,
    IN_PREPARATION,
    DISPATCHED,
    ON_WAY,
    AT_BRANCH,
    NEXT_DELIVERY,
    DELIVERED,
    CANCELLED,
    RETURN
}
